幫我把重要的架構資訊，寫入skills跟記憶md文件，以利之後使用claude code開發可記住重要資訊。

請幫我確認目前能否調用gcloud cli來操作GCP，目前專案應該是lkk-website-dev，前端使用app hosting 的newweb。後端使用 firestore


我的服務要從lkk-website-dev轉移到lkkdev，維持app hosting 的newweb。後端使用 firestore，目前 lkkdev 還有其他既有的服務，請不要去動到
// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyAqOFTADIc2d9CmZfe9C9oqJv92dHbFx1Q",
  authDomain: "lkkdev.firebaseapp.com",
  projectId: "lkkdev",
  storageBucket: "lkkdev.firebasestorage.app",
  messagingSenderId: "519198461380",
  appId: "1:519198461380:web:02167a9297dd17b714e891"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

請幫我把重要資訊寫回skills跟記憶
幫我commit push並release



lkkdev是我的開發環境，請幫我部署到正式環境


幫我回到dev











